# 复制此文件为 .env 并按需修改
# cp .env.example .env

# Session 加密密钥，生产环境请改为随机字符串
# 生成方式：python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=change-this-to-a-random-string

# 服务端口（本地运行时生效，云平台通常自动注入）
PORT=5000
