services:
  app:
    build: .
    ports:
      - "${PORT:-5000}:${PORT:-5000}"
    volumes:
      - ./data:/app/data
    environment:
      - SECRET_KEY=${SECRET_KEY:-dev-secret-change-in-production}
      - PORT=${PORT:-5000}
    restart: unless-stopped
